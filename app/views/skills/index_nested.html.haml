.main.span8
  %h1 現有哪些#{@instances.model_name.human}測驗
  = render @instances
  = paginate @instances

  .actions
    = link_to "所有 #{@instances.first.class.model_name.human}…", eval("#{controller_name}_path")
.sidebar.span4
  %h1 關於#{@instances.model_name.human}測驗的統計
